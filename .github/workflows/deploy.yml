name: Deploy Wiimu

on:
  push:
    branches:
      - main
    paths-ignore:
      - '/README.md'
      - '/.gitignore'
      - '/LICENSE'
      - '/requirements.txt'
      

# Push over SSH
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd ~/Wiimu

            git fetch origin
            git reset --hard origin/main

            source bin/activate

            pip install --upgrade pip
            pip install -r requirements.txt

            systemctl --user restart Wiimu
